# Images Folder

Current images in use:

- `teta1.png` - Main hero section image (currently in use)
- `teta2.png` - Meditation practice image (currently in use)

You can replace these images anytime. If images are not found, the site will automatically use fallback images from the internet.
